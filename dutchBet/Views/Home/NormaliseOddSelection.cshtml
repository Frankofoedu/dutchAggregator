@using Classes;
@model Classes.NormalisedSelection

@{
    ViewBag.Title = "NormaliseOddSelection";
    var Bet9jaOdds = (Bet9jaMatches)ViewBag.Bet9jaOdds;
    var BetPawaOdds = (DailyPawaMatches)ViewBag.BetPawaOdds;

    int max = ViewBag.Max;
}

<h2>NormaliseOddSelection</h2>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-6">
            @for (int i = 0; i < max; i++)
            {

            }
        </div>
        <div class="col-md-6">

        </div>
    </div>
</div>
